
export const SqlDefaultVal = {
  SELECT: '*',
  LIMIT_START: 0,
  LIMIT_COUNT: 30,
}

export enum SqlVal {
  SELECT = 'SELECT',
  WHERE = 'WHERE',
  GROUP_BY = 'GROUP_BY',
  ORDER_BY = 'ORDER_BY',
  LIMIT = 'LIMIT'
}