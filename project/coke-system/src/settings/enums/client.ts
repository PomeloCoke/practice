export enum ENV { Development, Production, Test, Sandbox }
export enum OS { Windows, Mac, Android, Ios, Other }
export enum DEVICE { PC, Mobile, Pad, Watch, Glass, Other }
export enum BROWSER { Chrome, Firefox, IE, Edge, Safari, Xiaomi, UC }

export default {
  ENV,
  OS,
  DEVICE,
  BROWSER
}
