enum ENV { Development, Production, Test, Sandbox }
enum OS { Windows, Mac, Android, Ios, Other }
enum DEVICE { PC, Mobile, Pad, Watch, Glass, Other }
enum BROWSER { Chrome, Firefox, IE, Edge, Safari, Xiaomi, UC }

export default {
  ENV,
  OS,
  DEVICE,
  BROWSER
}
